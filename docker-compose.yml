version: '3.8'

services:
  bot:
    build:
      context: .
      dockerfile: Dockerfile
    environment:
      BOT_TOKEN: ${BOT_TOKEN}
      DATABASE_URL: sqlite:telegram_bot.db
      RUST_LOG: info
    volumes:
      - ./data:/app
    restart: unless-stopped
    container_name: telegram-bot-rs
