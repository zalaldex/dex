services:
  - type: web
    name: telegram-bot-rs
    env: docker
    plan: free
    envVars:
      - key: RUST_LOG
        value: info
      - key: DATABASE_URL
        value: sqlite:telegram_bot.db
    healthCheckPath: /
    numInstances: 1
    autoDeploy: true
